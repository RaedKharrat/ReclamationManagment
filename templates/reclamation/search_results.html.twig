{% if reclamations is not empty %}
    <h2>Search Results:</h2>
    <ul>
        {% for reclamation in reclamations %}
            <li>{{ reclamation.recText }}</li>
            {# Display other properties of the Reclamation entity as needed #}
        {% endfor %}
    </ul>
{% else %}
    <p>No results found.</p>
{% endif %}