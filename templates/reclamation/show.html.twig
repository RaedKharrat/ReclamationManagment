{% extends 'back.html.twig' %}

{% block title %}Reclamation{% endblock %}

{% block body %}
    <h1>Reclamation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reclamation.id }}</td>
            </tr>
             <tr>
                <th>Subject</th>
                <td>{{ reclamation.recSubject }}</td>
            </tr>
            <tr>
                <th>Reclamation </th>
                <td>{{ reclamation.recText }}</td>
            </tr>
            <tr>
                <th>Date Reclamation</th>
                <td>{{ reclamation.dateRec ? reclamation.dateRec|date('Y-m-d') : '' }}</td>
            </tr>
           
        </tbody>
    </table>

    <a href="{{ path('app_reclamation_index') }}">back to list</a>

    <a href="{{ path('app_reclamation_edit', {'id': reclamation.id}) }}"style="color : green;">edit</a>

    {{ include('reclamation/_delete_form.html.twig') }}
{% endblock %}
