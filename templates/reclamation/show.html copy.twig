{% extends 'front.html.twig' %}

{% block title %}Reclamation{% endblock %}

{% block body %}
<section class="service_section layout_padding">
    <div class="container ">
      <div class="heading_container heading_center">
        <h2>My  <span>Reclamation</span></h2>
      </div>
      <div class="row">

   {% for reclamation in reclamations %}

 <div class="col-sm-6 col-md-4">
          <div class="box ">
            <div class="detail-box">
              {{reclamation.dateRec  ? reclamation.dateRec|date('Y-m-d') : ''}}
            </div>
            <div class="detail-box">
              <h5>
                {{reclamation.recSubject}}
              </h5>
              <p>
                {{reclamation.recText}}
              </p>
            </div>
              <a href="{{ path('app_reclamation_edit', {'id': reclamation.id}) }}"style="color : bleu; "><strong>edit</strong></a>
              

    {{ include('reclamation/_delete_form.html.twig') }}
          </div>
            </div>

        {% endfor %}
      </div>
   
    </div>
  </section>


{% endblock %}


